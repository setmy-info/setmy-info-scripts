Create simple Hello World script. In POSIX bourne shell. Name should be hello-world.sh
Should print out to std out "Hello Whole World!". Return 0 as script return result so next script knows that script sucsess result. Script should be ready for execution, that means execution bits should be set.
