#!/bin/sh

# Copyright (C) 2023 Imre Tabur <imre.tabur@mail.ee>

if [ -z "${SMI_HOME_PACKAGES_DIR}" ]; then
    SMI_HOME_LOCATION=$(smi-home-location)
    SMI_HOME_PACKAGES_DIR="${SMI_HOME_LOCATION}/packages"
fi

echo "${SMI_HOME_PACKAGES_DIR}"

exit 0
