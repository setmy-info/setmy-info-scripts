MCP_NAME=mcp
SYSTEM_PROFILES_DIR=$(smi-lib-location)/packages

mcp_download_func() {
    CUR_DIR=$(pwd)
    cd ${CUR_DIR}
}

mcp_install_func() {
    CUR_DIR=$(pwd)
    smi-prepare-venv ${SYSTEM_PROFILES_DIR}/${MCP_NAME}-requirements.txt
    cd ${CUR_DIR}
}
